"""srlib — shared helpers for the SKILL.md v8.0 (Phase 5c) scripts.

Modules: audit (hash-chained log entries), data (deterministic dataset
construction + ground-truth exclusion), metrics (deterministic agreement &
promotion statistics, stdlib only), modeling (seeded model/adapter plumbing).
"""
__version__ = "8.0.0"
