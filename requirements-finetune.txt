# Phase 5c (Learned Alignment) — pinned stack.
# Versions verified live on PyPI at v8 authoring (2026-07-10).
torch==2.13.0
transformers==5.13.0
datasets==5.0.0
accelerate==1.14.0
peft==0.19.1
trl==1.8.0
# bitsandbytes is CUDA-only: install it ONLY on an NVIDIA-GPU machine and
# only if you use "quantization": "4bit" (QLoRA). CPU / Apple-silicon users:
# skip this line and keep "quantization": "none" with a 1.5-3B base model.
bitsandbytes==0.49.2; platform_system == "Linux" or platform_system == "Windows"
